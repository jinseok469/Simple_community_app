{
  "extends": "expo/tsconfig.base",
  "compilerOptions": {
    "strict": true,
   "baseUrl": "app",      // 여기 추가!
    "paths": {
      "@/*": ["*"]
    }
  },
  "include": [
    "**/*.ts",
    "**/*.tsx",
    ".expo/types/**/*.ts",
    "expo-env.d.ts"
  ]
}
